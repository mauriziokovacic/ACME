def isbool(obj):
    """
    Returns whether or not the input is a bool

    Parameters
    ----------
    obj : object
        any object

    Returns
    -------
    bool
        True if the input is bool, False otherwise
    """

    return isinstance(obj,bool)
